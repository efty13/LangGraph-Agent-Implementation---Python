📘 LangGraph Python – Single-Node Greeting Agent

This project implements a simple LangGraph agent in Python that does not use any LLM.
The agent accepts a user’s name as input and returns a greeting message.
The implementation uses uv for dependency and environment management.

⸻

🚀 Features

	•	Single-node graph structure: START → greeting_node → END
	•	Minimal state schema: name → greeting
	•	Pure Python logic (no LLM integration)
	•	Clean and lightweight project structure
	•	Optional MCP server configuration
	•	Optional LangSmith tracing and visualization support
	•	Includes one example unit test (bonus)

